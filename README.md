# YGroup — Landing corporativa

Stack: **Vite + React + Tailwind + Framer Motion + Lucide**

## Desarrollo
```bash
npm install
npm run dev
```

## Build
```bash
npm run build
npm run preview
```

Edita `src/App.jsx` para el contenido, y `src/index.css` para estilos globales.
